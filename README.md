# Online Russian Checkers (Русские Шашки Онлайн)

Реалтайм веб-приложение для игры в русские шашки. Проект выполнен на React + TypeScript с использованием Firebase Firestore для синхронизации состояния.

**Демонстрация:** Работает полностью статически, легко деплоится на GitHub Pages / Vercel.

---

## 🚀 Возможности

### Игровая механика (Русские Правила)
- **Обязательное взятие:** Если есть возможность срубить шашку, другие ходы блокируются.
- **Взятие назад:** Можно рубить назад.
- **Дамки:**
  - "Летающие" дамки (ходят на любое число полей).
  - Превращение в дамку при проходе через дамочное поле (даже если ход продолжается).
- **Серии взятий:** Поддержка множественных взятий за один ход.
- **Авто-ничья:** При 60 полуходах (30 полных ходов) без взятий или движения пешек.

### Онлайн Функции
- **Мгновенные ссылки:** Создайте игру, скопируйте ссылку и отправьте другу. Игра начнется сразу после перехода.
- **Экран "Принять вызов":** Игрок, перешедший по ссылке, видит информацию о цвете и сопернике перед входом.
- **QR-код:** Генерация QR-кода для быстрого входа с мобильного телефона.
- **Зрители:** Если в комнате уже 2 игрока, третий подключается как наблюдатель.
- **Реванш:** После окончания партии можно предложить реванш (цвета меняются автоматически).
- **Синхронизация:** Оптимистичное обновление UI + Firestore real-time listeners.

### PVE (Игра с Ботом)
- **Умный бот (Minimax + Quiescence Search):** Бот просчитывает ходы на глубину и дополнительно анализирует цепочки взятий, чтобы избегать тактических ошибок.

---

## 🛠 Технологический стек

*   **Frontend:** React 18, TypeScript, Tailwind CSS.
*   **Сборка:** Vite (ES Modules).
*   **Backend:** Firebase Firestore (NoSQL, Realtime).
*   **Деплой:** GitHub Pages (полностью статический).

---

## 📂 Структура Проекта

```
/
├── index.html          # Точка входа
├── index.tsx           # Монтирование React
├── App.tsx             # Главный контроллер игры (роутинг, состояние)
├── types.ts            # TypeScript интерфейсы
├── components/         # UI Компоненты
│   ├── Board.tsx       # Игровая доска (Drag & Drop, анимации)
│   ├── GameLobby.tsx   # Экран ожидания создателя
│   └── JoinGameScreen.tsx # Экран принятия вызова
├── rules/
│   └── checkersRules.ts # Чистая логика проверки ходов (валидация)
├── services/
│   ├── firebase.ts     # Конфигурация Firebase
│   └── gameService.ts  # Методы API (createGame, joinGame, move)
└── ai/
    └── bot.ts          # Логика бота
```

---

## 🏁 Запуск локально

1.  **Установите зависимости:**
    ```bash
    npm install
    ```

2.  **Запустите сервер разработки:**
    ```bash
    npm run dev
    ```
    Откройте `http://localhost:5173`.

---

## ⚙️ Настройка Firebase

В файле `services/firebase.ts` находится конфигурация. По умолчанию используется открытый тестовый проект. Для продакшена:

1.  Создайте проект на [console.firebase.google.com](https://console.firebase.google.com).
2.  Создайте Web App и получите конфиг.
3.  Создайте базу данных **Firestore**.

### Настройка удаления старых игр (TTL)

Чтобы база данных не засорялась, настройте автоматическое удаление игр через 24 часа:

1.  Перейдите в **Google Cloud Console** вашего проекта (или через Firebase Console -> Firestore -> Data).
2.  Найдите вкладку **Time-to-live (TTL)**.
3.  Нажмите **Create Policy**.
4.  В поле **Collection group** введите: `games`.
5.  В поле **Timestamp field** введите: `expireAt`.
6.  Нажмите **Create**.

Теперь Firestore будет автоматически удалять игры, у которых поле `expireAt` (дата создания + 24ч) наступило. Никаких скриптов писать не нужно.

---

## 🧪 Тестирование (Checklist)

1.  **Создание игры:**
    *   Нажать "Белые" -> Лобби должно писать "Вы Белые".
    *   Нажать "Черные" -> Лобби должно писать "Вы Черные".
    *   Нажать "Случайно" -> Лобби показывает выпавший цвет.
2.  **Приглашение:**
    *   Скопировать ссылку из Лобби.
    *   Открыть в режиме Инкогнито.
    *   Должен появиться экран "Принять вызов" с правильным цветом (противоположным создателю).
3.  **Игра:**
    *   Ходы отображаются у обоих игроков.
    *   Взятие обязательно (другие ходы не работают).
    *   Дамка ходит по диагонали на всю длину.
4.  **Конец игры:**
    *   При отсутствии ходов или шашек -> Победа соперника.
    *   Кнопка "Реванш" создает новую комнату и меняет цвета.
```