# Online Russian Checkers (Русские Шашки Онлайн)

Реалтайм веб-приложение для игры в русские шашки. Проект выполнен на React + TypeScript с использованием Firebase Firestore для синхронизации состояния.

**Демонстрация:** Работает полностью статически, легко деплоится на GitHub Pages / Vercel.

---

## 🚀 Возможности

### Игровая механика (Русские Правила)
- **Обязательное взятие:** Если есть возможность срубить шашку, другие ходы блокируются.
- **Взятие назад:** Можно рубить назад.
- **Дамки:**
  - "Летающие" дамки (ходят на любое число полей).
  - Превращение в дамку при проходе через дамочное поле (даже если ход продолжается).
- **Серии взятий:** Поддержка множественных взятий за один ход.
- **Авто-ничья:** При 60 полуходах (30 полных ходов) без взятий или движения пешек.

### Онлайн Функции
- **Мгновенные ссылки:** Создайте игру, скопируйте ссылку и отправьте другу. Игра начнется сразу после перехода.
- **Экран "Принять вызов":** Игрок, перешедший по ссылке, видит информацию о цвете и сопернике перед входом.
- **QR-код:** Генерация QR-кода для быстрого входа с мобильного телефона.
- **Зрители:** Если в комнате уже 2 игрока, третий подключается как наблюдатель.
- **Реванш:** После окончания партии можно предложить реванш.
    - Если соперник предложил реванш, ваша кнопка станет зеленой и начнет пульсировать.
    - Цвета меняются автоматически.
- **Возврат хода (Undo):**
    - Можно попросить вернуть ход обратно.
    - Соперник видит уведомление и может принять или отклонить запрос.
- **Синхронизация:** Оптимистичное обновление UI + Firestore real-time listeners.

### PVE (Игра с Ботом)
- **Умный бот (Minimax + Quiescence Search):** Бот просчитывает ходы на глубину и дополнительно анализирует цепочки взятий.

---

## 🛠 Технологический стек

*   **Frontend:** React 18, TypeScript, Tailwind CSS.
*   **Сборка:** Vite (ES Modules).
*   **Backend:** Firebase Firestore (NoSQL, Realtime).
*   **Деплой:** GitHub Pages (полностью статический).

---

## 🏁 Деплой (Инструкция)

### 1. Подготовка
Убедитесь, что вы закоммитили все изменения:
```bash
git add .
git commit -m "Final updates for release"
git push
```

### 2. Сборка и Деплой
В проекте уже настроен скрипт для деплоя на GitHub Pages.

Выполните команду:
```bash
npm run deploy
```

**Что произойдет:**
1.  Vite соберет проект в папку `dist`.
2.  Пакет `gh-pages` отправит содержимое папки `dist` в ветку `gh-pages` вашего репозитория.

После завершения (через 1-2 минуты) сайт будет доступен по ссылке вида:
`https://<ваш-юзернейм>.github.io/<название-репозитория>/`

### 3. Настройка Firebase (Важно для продакшена)
В `services/firebase.ts` используются открытые ключи. Для стабильной работы:
1.  Создайте свой проект на [console.firebase.google.com](https://console.firebase.google.com).
2.  Создайте **Firestore Database** (Start in production mode).
3.  Настройте **Rules** (Правила безопасности):
    ```
    rules_version = '2';
    service cloud.firestore {
      match /databases/{database}/documents {
        match /games/{gameId} {
          allow read, write: if true; // В MVP открыт доступ, для продакшена лучше ограничить
        }
      }
    }
    ```
4.  Замените конфиг в `services/firebase.ts` на свой.

### Настройка TTL (Автоудаление старых игр)
Чтобы база не переполнялась:
1.  В Google Cloud Console (Firestore) перейдите в **Time-to-live (TTL)**.
2.  Нажмите **Create Policy**.
3.  Collection group: `games`.
4.  Timestamp field: `expireAt`.
5.  Нажмите **Create**.
